PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/pbls_rescue_path

# Step 2a only: stands in for participant auth until /auth/play/login exists.
# Must be the _id of a real, non-deleted participants document.
DEV_PARTICIPANT_ID=